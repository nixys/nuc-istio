{{- range $name := keys ($.Values.destinationrules | default dict) | sortAlpha }}
{{- $destinationrule := get $.Values.destinationrules $name -}}
{{- if ne $name "__helm_docs_example__" }}
{{ include "nuc-istio.renderDestinationRule" (dict "root" $ "name" $name "item" $destinationrule) }}
{{- end }}
{{- end }}
